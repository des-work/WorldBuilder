<Window x:Class="Genisis.Presentation.Wpf.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Input" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner"
        Background="#FF2D2D30"
        MinWidth="300"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="MinWidth" Value="75"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="15">
        <TextBlock x:Name="PromptText" Foreground="White" Margin="0,0,0,10" TextWrapping="Wrap"/>
        <TextBox x:Name="ResponseTextBox"
                 Background="#252526" Foreground="White" BorderThickness="1"
                 BorderBrush="#FF555555"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" IsDefault="True" Click="OkButton_Click"
                    Background="#27ae60" Foreground="White" BorderThickness="0"/>
            <Button Content="Cancel" IsCancel="True"
                    Background="#34495e" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </StackPanel>
</Window>
