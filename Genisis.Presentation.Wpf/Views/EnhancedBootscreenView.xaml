<UserControl x:Class="Genisis.Presentation.Wpf.Views.EnhancedBootscreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    
    <UserControl.Resources>
        <!-- Progress Animation -->
        <Storyboard x:Key="ProgressAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="ProgressBar"
                           Storyboard.TargetProperty="Opacity"
                           From="0.3" To="1" Duration="0:0:1"
                           AutoReverse="True"/>
        </Storyboard>

        <!-- Loading Text Animation -->
        <Storyboard x:Key="LoadingTextAnimation" RepeatBehavior="Forever">
            <StringAnimationUsingKeyFrames Storyboard.TargetName="LoadingText"
                                         Storyboard.TargetProperty="Text">
                <DiscreteStringKeyFrame KeyTime="0:0:0" Value="Loading"/>
                <DiscreteStringKeyFrame KeyTime="0:0:0.5" Value="Loading."/>
                <DiscreteStringKeyFrame KeyTime="0:0:1" Value="Loading.."/>
                <DiscreteStringKeyFrame KeyTime="0:0:1.5" Value="Loading..."/>
            </StringAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>

    <Grid Background="Black">
        <!-- Main Canvas for Bootscreen Elements -->
        <Canvas x:Name="MainCanvas" 
                Background="Transparent"
                Width="800" Height="600"/>
        
        <!-- Progress Indicator -->
        <StackPanel HorizontalAlignment="Center" 
                    VerticalAlignment="Bottom" 
                    Margin="0,0,0,50">
            <TextBlock x:Name="LoadingText"
                       Text="Loading"
                       FontSize="16" 
                       Foreground="LightGray" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            
            <ProgressBar x:Name="ProgressBar"
                         Width="300" 
                         Height="6" 
                         IsIndeterminate="True"
                         Foreground="Cyan"
                         Background="DarkGray"
                         BorderThickness="0"/>
            
            <!-- Progress Text -->
            <TextBlock x:Name="ProgressText"
                       Text="Initializing..."
                       FontSize="12" 
                       Foreground="Gray" 
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Theme-specific overlay effects -->
        <Canvas x:Name="OverlayCanvas" 
                Background="Transparent"
                Width="800" Height="600"/>
    </Grid>
</UserControl>
